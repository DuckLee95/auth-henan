# 贡献指南

## 贡献一个新学校

各校社长在校内分配对应学校的开发负责人，并将负责人的 GitHub 用户名发给豫高联皮肤站负责人。负责人接受仓库协作者邀请后，确认学校、认证入口和改动范围即可开始；需要跨学校协调时，由各校社长与豫高联皮肤站负责人沟通。任何沟通渠道中都不应发送测试账号或密码。

开发与提交采用协作者分支流程：

1. 接受 GitHub 仓库协作者邀请并克隆本仓库。
2. 创建自己的功能或修复分支，不要直接修改 `main`。
3. 完成自己负责学校的代码修改。
4. 将功能分支推送到本仓库，并向 `main` 提交 Pull Request。
5. 确认改动内容后自行合并。

### 1. 实现 Provider

在 `src/Schools` 中新增一个认证类并实现 `SchoolAuth`：

```php
<?php

namespace Blessing\HAuth\Schools;

class ExampleAuth implements SchoolAuth
{
    public function login(string $username, string $password): bool
    {
        // 调用学校统一认证系统并判断结果。
    }
}
```

`login()` 的返回约定：

- 验证成功：返回 `true`。
- 账号或密码错误：返回 `false`。
- 网络故障、学校页面改版或无法判断结果：抛出异常，并提供不含凭据的错误信息。

### 2. 注册学校

在 `src/SchoolRegistry.php` 的 `SCHOOLS` 中添加学校标识、显示名称、皮肤站邮箱后缀和 Provider 类。

学校标识应使用简短、稳定的小写英文缩写，例如 `ncwu`、`zzu`。

### 3. 提交与合并

Pull Request 至少应说明：

- 适配的学校及认证入口。
- 学校认证存在验证码、二次认证等限制时简单说明。

可以提供脱敏日志或截图，但不得提交任何真实凭据和会话数据。

## 分工与后续维护

各校社长负责确定或更换本校 Provider 的开发负责人。负责人需要：

- 跟进本校统一认证系统的页面、接口和加密方式变化。
- 处理分配给自己的 Provider 开发和修复任务。
- 在发布前完成必要的真实环境验证。
- 无法继续维护时及时告知本校社长，由社长重新分配。

首次学校适配或修复合并后，作者会加入 `README.md` 的贡献者名单。后续更新仍使用独立分支和 Pull Request，由各校负责人自行确认并合并。

## 其他贡献

修复问题和改进文档同样欢迎。提交贡献时，可以在同一个 Pull Request 中更新 `README.md` 的贡献者名单，写明自己的主要贡献。
